# Runtime

Reserved implementation entry.

Expected surface:

```
validate(scene.json)
render(scene.json)
```

Runtime responsibilities:

- consume validated scene
- render animation
- produce mp4
- produce evidence

Runtime must not plan scenes.
